apiVersion: v1
kind: Service
metadata:
  name: {{ include "ccdb5-api.fullname" . }}
  labels:
    {{- include "ccdb5-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ccdb5-api.selectorLabels" . | nindent 4 }}
