apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ccdb5-api.fullname" . }}-test-connection"
  labels:
    {{- include "ccdb5-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ccdb5-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
